//  arrays.js
const steps = ["one", "two", "three"];
const listTemplate(step)  => { steps.map(steps)
  return //the html string made from step
}
const stepsHtml = listTemplate; // use map to convert the list from strings to HTML
document.querySelector("#myList").innerHTML = stepsHtml;// set the innerHTML